<!DOCTYPE html>
<html lang="it">
  <head>
    <meta charset="UTF-8">
    <title>{{ shop.name }} - {{ page_title }}</title>
    {{ content_for_header }}
    <link rel="stylesheet" href="{{ 'style.css' | asset_url }}">
  </head>
  <body>
    {% section 'header' %}
    <main>
      {{ content_for_layout }}
    </main>
    {% section 'footer' %}
  </body>
</html>
